version: 2

models:
  - name: obt_order_fulfillment
    description: The order fulfillment fact table combined with all of its outer-joined dimensions into one bit table. Transaction fact table with one row per order does not include product.
